# scripts

prerender.mjs emits per-route HTML and sitemap. verify-site.mjs checks headings, metadata and local links/assets. render.swift optionally captures native WebKit snapshots on macOS.

`render.swift` also reports BendMe image containment and control separation before and after switching lid states.
